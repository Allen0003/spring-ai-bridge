

demo url:
http://localhost:8080/index.html
demo url:
http://localhost:8080/api/ai/initData


2. 金融知識 RAG 檢索

利用向量化技術將交易處理 SOP 存儲，讓營運人員能以自然語言查詢：「User64 的交易為何失敗？」AI 將比對 SOP 給出精準解答。

階段	任務內容	關鍵技術	狀態
Week 1	環境與基礎串接	Spring AI, Ollama/OpenAI	K
Week 2	Function Calling 實作	AI Agent 調用微服務 API	K
Week 3	RAG 向量檢索整合	pgvector / Milvus, Embedding	K
Week 4	AIOps 自動化部署	AWS EKS, KEDA Integration	⏳ 待處理

玩法二：AI 異常分析 Agent (實戰價值最高)  K

1. 玩法三：自動化決策與處置 (Action-Oriented Agent)

目標：讓 AI 根據風險評估，主動要求執行「凍結卡片」或「調整限額」。

實作細節：

在 TransactionTools 增加一個 @Tool("暫時鎖定指定用戶的信用卡") 方法。

在 System Message 告訴喵探員：「如果風險分數超過 8 分，請先詢問人類是否需要喵幫您鎖卡」。

    價值：這展現了 AI 從 感知 (Perception) 到 行動 (Action) 的完整閉環。

2. 玩法四：多模態分析 (Vision for Fraud Detection)

如果用戶收到一張奇怪的簡訊截圖或是 ATM 報錯畫面，喵探員能不能看圖說故事？

目標：上傳圖片給喵探員分析。

    實作細節：

        利用 Gemini 1.5 Flash 的多模態能力（Vision）。

        前端 index.html 增加上傳圖片功能。

        後端傳送 ImageContent 給 Gemini。

    場景：用戶上傳一張「銀行釣魚簡訊」截圖，喵探員看一眼就說：「喵！！這是詐騙網址，千萬別點！」。




地端跑 model 超級久

打算 串網路免費的


week2 -> 用 local model

1. 第一輪 (LLM)：AI 分析問題，回傳一個 ToolCall 指令（這步最久）。

2. 執行 (Java)：你的 getTransactionDetails 被呼叫（這步你應該會看到 System.out.println）。

3. 第二輪 (LLM)：AI 拿到結果，組織成人類語言回答（這步也要等）

week2 -> 改成用雲端 google AI

AI 讓你的 Java 程式去執行 TransactionTools 裡的查庫邏輯
AI 負責決策（思考要用哪個工具），你的微服務負責執行。

變超快





week3 前置補充



補充:

Postgres 是（關聯式) SQL
AI（特別是 LLM / RAG）的應用中，我們需要儲存「向量」（Embedding）


CREATE TABLE my_knowledge_base (
    id serial PRIMARY KEY,
    content text,           -- 原始文字（給人看的）
    embedding vector(1536)  -- 向量數據（給 AI 搜尋用的）
);



體型 兇猛程度

INSERT INTO my_knowledge_base (id, name, embedding)
VALUES (1, '小貓', '[0.1, 0.1]');

INSERT INTO my_knowledge_base (id, name, embedding)
VALUES (2, '貓', '[0.2, 0.2]');

INSERT INTO my_knowledge_base (id, name, embedding)
VALUES (3, '螞蟻', '[0.01, 0.01]');



數字通常不是手動定義的

交給模型： 你把「小貓」這兩個字傳給 OpenAI 或 HuggingFace 的 Embedding 模型


搜尋時的樣子

SELECT name, embedding <=> '[0.12, 0.15]' AS distance
FROM my_knowledge_base
ORDER BY distance
LIMIT 2;




Milvus 處理上億規模 是 分散式的 Elasticsearch 或 「專為向量設計的雲原生運算引擎」

不需要手動去連資料庫。 Milvus 參數搞不太定 放棄




Week 3 的核心任務

1
文件向量化 (Embedding)
將文字轉為座標

選定一段金融條文，使用 GoogleAiEmbeddingModel 將文字轉換成一串數字（向量）。這串數字代表了這段話的「語意」。
2
儲存至向量資料庫
pgvector / Milvus

將向量存入資料庫。這不是傳統的關鍵字比對，而是「語意空間」的儲存。
3
相似度檢索 (Vector Search)
找出最相關的條文

當用戶問「洗錢防制法如何規定？」時，程式會先去資料庫撈出最接近的 3 段法規。
4
增強生成 (Augmented Generation)
餵給 Gemini 回答

將撈出的法規內容併入 Prompt 告訴 Gemini：「請根據以下參考資料回答：[法規內容]」。

簡單的說先把資料轉成向量 (AI自己去轉) 然後在用query 去查向量


*** 增強

1. 增加 大腦設定 -> 寫更好的 Prompt Engineering 讓 AI 把各處拿到的片段資料 ex: tool, rag


*** 生成

從各處拿完資料 然後 AI 組成之後再回傳

*** 記憶

就是把之前的對話都當成 prompt 一起丟進去





*** tool

AI 會自己決定用哪個 tool 精準度取決於 tool 的描述  不用 if / else 程式也自己去做事

在 agent build 的時候告訴他要用 tool

你不需要寫任何 if-else 去判斷要呼叫哪個工具。你只需要：

寫好工具。寫好 @Tool 的中文描述。把工具餵給 AiServices。

剩下的「選擇」與「調用」工作，Gemini 和 LangChain4j 會幫你搞定




在 langchain config 裡面 用@bean
去實作 agent


